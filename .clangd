CompileOptions:
	Add: [-Wall, -Wextra]

If:
	PathMatch: .*\.c
CompileOptions:
	Add: [-xc, -std=c99]

If:
	PathMatch: .*\.h
CompileFlags:
	Add: [-xc-header]

If:
	PathMatch: .*\.(cc|cpp)
CompileOptions:
	Add: [-xcpp, -std=c++23]

